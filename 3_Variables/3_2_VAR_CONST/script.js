const nome = "pedro"; // usado para declarar valores que não devem sofrer alteração durante o código
var sobrenome = "henrique"; // jeito antigo para declarar variáveis, apenas use "LET" ou "CONST"

console.log(nome);
console.log(sobrenome);